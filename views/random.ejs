<%- include('partials/head') %>

<body>
    <%- include('partials/nav') %>  
    
    <h1> Your random number is: <%= rand %> </h1>
    <% if(rand % 2 === 0) { %>

        <h2>That is a even number!</h2>

     <% } else { %>

        <h2>That is a odd number, bitch!</h2>

     <% } %> 
     <h3> That number is: <%=rand%2===0 ? 'EVEN!' : 'ODD!' %> </h3>        
</body>
</html>